{
  "properties": {
    "behavior_port": {
      "description": "The COM port of the Harp Behavior.",
      "pattern": "^COM\\d+$",
      "title": "Behavior Port",
      "type": "string"
    },
    "soundcard_port": {
      "description": "The COM port of the Harp SoundCard.",
      "pattern": "^COM\\d+$",
      "title": "Soundcard Port",
      "type": "string"
    },
    "left_pump_port": {
      "description": "The COM port of the left Harp SyringePump.",
      "pattern": "^COM\\d+$",
      "title": "Left Pump Port",
      "type": "string"
    },
    "right_pump_port": {
      "description": "The COM port of the right Harp SyringePump.",
      "pattern": "^COM\\d+$",
      "title": "Right Pump Port",
      "type": "string"
    },
    "currentdriver_port": {
      "description": "The COM port of the Harp CurrentDriver.",
      "pattern": "^COM\\d+$",
      "title": "Currentdriver Port",
      "type": "string"
    },
    "animal_path": {
      "description": "The path to the animal.yml configuration file.",
      "title": "Animal Path",
      "type": "string"
    },
    "setup_path": {
      "description": "The path to the setup.json configuration file.",
      "title": "Setup Path",
      "type": "string"
    },
    "training_path": {
      "description": "The path to the training.yml configuration file.",
      "title": "Training Path",
      "type": "string"
    },
    "output_path": {
      "description": "The path to the output directory, where the output date will be saved.",
      "title": "Output Path",
      "type": "string"
    }
  },
  "required": [
    "behavior_port",
    "soundcard_port",
    "left_pump_port",
    "right_pump_port",
    "currentdriver_port",
    "animal_path",
    "setup_path",
    "training_path",
    "output_path"
  ],
  "title": "Config",
  "type": "object"
}