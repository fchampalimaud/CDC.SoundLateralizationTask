{
  "definitions": {
    "Line": {
      "properties": {
        "condition": {
          "description": "The condition that filters the data to be plotted.",
          "title": "Condition",
          "type": "string"
        },
        "marker": {
          "description": "The marker to be used.",
          "enum": [
            "<",
            ">",
            "o",
            "o-"
          ],
          "title": "Marker",
          "type": "string"
        },
        "color": {
          "description": "The color to be used.",
          "title": "Color",
          "type": "string"
        }
      },
      "required": [
        "condition",
        "marker",
        "color"
      ],
      "title": "Line",
      "type": "object"
    }
  },
  "properties": {
    "name": {
      "description": "The name of the plot.",
      "title": "Name",
      "type": "string"
    },
    "x": {
      "description": "The x coordinate of the plot.",
      "maximum": 2,
      "minimum": 0,
      "title": "X",
      "type": "integer"
    },
    "y": {
      "description": "The y coordinate of the plot.",
      "maximum": 2,
      "minimum": 0,
      "title": "Y",
      "type": "integer"
    },
    "xlabel": {
      "description": "The label of the x-axis of the plot.",
      "title": "Xlabel",
      "type": "string"
    },
    "ylabel": {
      "description": "The label of the y-axis of the plot.",
      "title": "Ylabel",
      "type": "string"
    },
    "lines": {
      "description": "Contains the parameters for each line in the plot.",
      "items": {
        "$ref": "#/definitions/Line"
      },
      "title": "Lines",
      "type": "array"
    }
  },
  "required": [
    "name",
    "x",
    "y",
    "xlabel",
    "ylabel",
    "lines"
  ],
  "title": "Plot",
  "type": "object"
}